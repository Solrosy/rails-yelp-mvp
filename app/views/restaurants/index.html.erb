
<div class="d-flex justify-content-between align-items-center">
<h1>Restaurants</h1>
<%= link_to "Add", new_restaurant_path, class: "btn btn-primary px-5 my-3" %>
</div>


  <div class="row">
    <% @restaurants.each do |restaurant| %>
      <%= render "restaurant_card", restaurant: restaurant %>
    <% end %>
    </div>
